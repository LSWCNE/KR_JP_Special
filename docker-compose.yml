services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./sample_data:/app/sample_data
      - ./kj_exchange.db:/app/kj_exchange.db
